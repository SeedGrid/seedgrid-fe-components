// Barrel client-safe (@seedgrid/fe-subdomain-tenancy). Tipos + paths. Server em /server.
// A tela de signup do tenant é BR-coupled (CNPJ/ViaCEP/UF) → SCAFFOLD (app owns).

export * from "./tenant-signup";
export * from "./paths";
export * from "./subdomain-rules";
