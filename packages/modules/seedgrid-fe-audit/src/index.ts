// Barrel client-safe (@seedgrid/fe-audit). Tipos + paths. Server helper em /server.
// A tela de busca de audit é SCAFFOLD (app owns), entregue pelo CLI — não é da lib.

export * from "./audit";
export * from "./paths";
