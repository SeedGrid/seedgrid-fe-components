// Barrel client-safe (@seedgrid/fe-apikeys). Tipos + paths. Server helper em /server.
// A tela de gestão de API keys é SCAFFOLD (app owns), entregue pelo CLI.

export * from "./apikeys";
export * from "./paths";
